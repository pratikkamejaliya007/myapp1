import React from 'react'
import { useRef , useEffect ,useState } from 'react';

function Data() {
    // const inputRef = useRef(0);

    // function headler(){

    //   inputRef.current = inputRef.current + 1

    //   alert("Btn clicked" + inputRef.current )

    // }
    
    return (
      <div>
        
        {/* <button onClick={headler}>Click</button> */}

      </div>
    );
}

export default Data